apiVersion: v1
kind: Service
metadata:
  name: harbor-preheat-webhook
  namespace: preheat
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: harbor-preheat-webhook