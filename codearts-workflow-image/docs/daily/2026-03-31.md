# Daily Summary - 2026-03-31

## Project: codearts-workflow-image

### 1. Feature: Copy-Script Approach

Replacing old copy-pod logic with copy-script approach.

**Goal:**
- Use `volumeClaimTemplates` instead of `persistentVolumeClaim` volumes
- Copy script: `swr..../alpine:3.23.3` image
- Pattern: `trap TERM + sleep infinity`
- VolumeClaimTemplates name: `output`
- MountPath: `CP_artifacts_temp_folder`
- Copy script echoes "copying artifacts...."

**Implementation** (`convert_script_to_argo.go`):
- Added copy-script template generation
- Main steps template: main-script → copy-script
- volumeClaimTemplates with name `output`
- Copy-script source with trap and sleep infinity

### 2. Test Case Updates

| Test | Changes |
|------|---------|
| test10-cp-artifacts | Updated expected.yaml, deleted old PVC/copy-pod files |
| test13-cp-artifacts-v2 | Updated expected.yaml, deleted old PVC/copy-pod files, created workflow_templatev2.yaml |

### 3. YamlEqual Fixes

- Added `sort.Strings(keys)` for map key sorting
- Added `strings.TrimRight(val, "\n")` for trailing newline normalization

**YAML Block Scalar Chomping:**
- `|`: clip - single newline kept
- `|-`: strip - no newline

### 4. Argo Workflow Test Script Updates

**main.sh changes:**
- Unified `wait_for_pod()` function
- Copy-script detection via `CP_artifacts_temp_folder`
- Main-script wait runs for ALL workflows

**Pod naming fix:**
- `main-script` OR equals `workflow_name`

### 5. Renamed Files

| From | To |
|------|-----|
| `workflow_stuck.yaml` | `workflow_copy.yaml` |
| `stuck-script` template | `copy-script` |

### 6. Files Modified

- `go/cmd/converter/package/convert_script_to_argo.go`
- `go/cmd/converter/case/newtest/test10-cp-artifacts/expected.yaml`
- `go/cmd/converter/case/newtest/test13-cp-artifacts-v2/expected.yaml`
- `go/cmd/converter/convertv2_to_yaml_test.go`
- `.opencode/skills/submit-test/scripts/main.sh`
- `src/unit.sh`

### 7. Files Deleted

- `expected-copy-pod.yaml` (test10, test13)
- `expected-artifact-pvc.yaml` (test10, test13)

### 8. Test Results

**All 12 Go tests PASS** ✅
**All 12 Argo workflow tests PASS** ✅

---

## Mistake Notebook Entries (2026-03-30)

1. Wrong test case naming (home-* vs test*N-*)
2. Used wrong image in copy pod
3. Added CP_cp_image to non-home case
4. Wrong CP_runs_on format
5. Expected files didn't match generated output
6. Wrong PVC name in eval.sh
7. Wrong resource validation in eval.sh
8. Removed wrong test case
9. Dirty `$` in clean.sh
10. JQ query didn't check nested volumeMounts