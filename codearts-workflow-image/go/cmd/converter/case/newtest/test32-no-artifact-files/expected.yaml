apiVersion: batch.volcano.sh/v1alpha1
kind: Job
metadata:
    generateName: testorg-testrepo-test32-
    labels:
        jobPRID: "113"
        jobRepositoryName: testorg-testrepo-test32
        kubernetes.io/arch: amd64
        pipeline/run-id: test-no-artifact-files-123
spec:
    policies:
        - event: PodFailed
          action: AbortJob
    queue: shared-flexible-queue
    maxRetry: 1
    minAvailable: 1
    ttlSecondsAfterFinished: 1800
    tasks:
        - name: main-script
          replicas: 1
          maxRetry: 1
          template:
            spec:
                containers:
                    - name: ascend
                      image: swr.cn-southwest-2.myhuaweicloud.com/base_image/ascend-ci/cann:8.2.rc1-910b-ubuntu22.04-py3.11
                      command:
                        - bash
                        - -c
                      args:
                        - |-
                          (trap 'EXIT_CODE=$?; [ "$EXIT_CODE" -ne 0 ] && sleep 10' EXIT
                          #!/bin/sh
                          echo "Running without artifact files..."
                          echo "No files match artifact pattern"
                          echo "Done"

                          cd ${WORKSPACE}
                          cp -r --parents *.txt *.log /output/
                          ); _ec=$?; echo $_ec > /output/.ascend-done; exit $_ec
                      workingDir: /workspace
                      resources:
                        limits:
                            cpu: "1"
                            memory: 1Gi
                        requests:
                            cpu: "1"
                            memory: 1Gi
                      volumeMounts:
                        - name: output
                          mountPath: /output
                      env:
                        - name: WORKSPACE
                          value: /workspace
                        - name: workspace
                          value: /workspace
                        - name: GOPROXY
                          value: http://cache-service.nginx-pypi-cache.svc.cluster.local:8084|https://goproxy.cn|direct
                        - name: GOSUMDB
                          value: sum.golang.google.cn
                        - name: MAX_JOBS
                          value: "1"
                        - name: CMAKE_BUILD_PARALLEL_LEVEL
                          value: "1"
                        - name: JOB_ID
                          value: job-no-artifact-files
                        - name: BUILDNUMBER
                          value: "323334353"
                    - name: copy-artifact
                      image: swr.cn-southwest-2.myhuaweicloud.com/modelfoundry/alpine:3.23.3
                      command:
                        - sh
                      args:
                        - -c
                        - |-
                          trap 'echo "Received TERM, exiting."; exit 0' TERM
                          TIMER_FILE="/tmp/reset_timer"
                          TIMEOUT_SEC=600
                          touch "$TIMER_FILE"
                          COUNT=0
                          while true; do
                            if [ -f "$DONE_FILE" ]; then
                              _ascend_exit=$(cat "$DONE_FILE" 2>/dev/null || echo "1")
                              echo "ascend container finished (exit $_ascend_exit)."
                              if [ "$_ascend_exit" != "0" ]; then
                                echo "ascend failed, exiting sidecar."
                                exit 0
                              fi
                            fi
                            LAST=$(stat -c %Y "$TIMER_FILE" 2>/dev/null || stat -f %m "$TIMER_FILE")
                            NOW=$(date +%s)
                            ELAPSED=$((NOW - LAST))
                            if [ "$ELAPSED" -ge "$TIMEOUT_SEC" ]; then
                              echo "Timer expired. Exiting."
                              exit 0
                            fi
                            COUNT=$((COUNT + 1))
                            if [ $((COUNT % 5)) -eq 0 ]; then
                              REMAINING=$((TIMEOUT_SEC - ELAPSED))
                              echo "Timer remaining: ${REMAINING}s"
                            fi
                            sleep 1
                          done
                      resources:
                        limits:
                            cpu: "2"
                            memory: 1Gi
                        requests:
                            cpu: 200m
                            memory: 200Mi
                      volumeMounts:
                        - name: output
                          mountPath: /output
                      env:
                        - name: DONE_FILE
                          value: /output/.ascend-done
                        - name: WORKSPACE
                          value: /workspace
                nodeSelector:
                    kubernetes.io/arch: amd64
                imagePullSecrets:
                    - name: huawei-swr-image-pull-secret-model-gy
                activeDeadlineSeconds: 14400
                securityContext:
                    runAsUser: 0
                volumes:
                    - name: output
                      emptyDir:
                        sizeLimit: 10Gi
                restartPolicy: Never
