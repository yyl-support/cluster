apiVersion: v1
kind: Service
metadata:
  name: git-cache-http-server
  labels:
    app: git-cache-http-server
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: git-cache-http-server